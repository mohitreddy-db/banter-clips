# Base URL of the BanterClips FastAPI backend.
# Local: the uvicorn dev server. Production: the droplet's https domain
# (set this in Vercel → Project → Settings → Environment Variables).
VITE_API_URL=http://localhost:8000
